{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "find / -name 'start.sh' 2>/dev/null && sleep 30",
    "restartPolicyType": "ON_FAILURE"
  }
}
